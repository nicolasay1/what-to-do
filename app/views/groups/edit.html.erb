<h1>Page to be built!</h1>


 <div class="row">
    <div class="col">
        <% if current_user.memberships.find_by(group: @group, admin: true) %>
          <%= link_to "Click To Delete Group", group_path(@group), data: { "turbo-method": :delete }, class: "btn btn-danger text-white"%>
        <% end %>
    </div>
  </div>


  <div>
  <h2> Group members </h2>
    <%= link_to 'Leave Group', group_membership_path(@group, membership: {group: @group, user_id: current_user}),data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}, class: "btn btn-danger text-white"%>

  <% @members.each do |member| %>
    <li> <%= member.email %>
      <% if current_user.memberships.find_by(group: @group, admin: true) %>
        <%= link_to group_membership_path(@group, membership: {group: @group, user_id: member}),data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} do %>
        <i class="fa fa-trash" aria-hidden="true"></i>
        <% end %>
      <% end %>
    </li>
  <% end%>
  </div>
<div>
